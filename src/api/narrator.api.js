/* import { Narrator } from "../Narrator.mjs"

const narrator = new Narrator
console.log("El content script del narrator ha sido cargado")

chrome.runtime.sendMessage({ action: 'fetchState' }, (state) => {
    console.log(state)

    console.log(player)
    player.isPlaying = state.playing
    player.isPaused = state.paused
    player.isStopped = state.stopped
    console.log(player)

}) */